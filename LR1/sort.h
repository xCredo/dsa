#ifndef sort
#define sort

#include <stdint.h>
void SelectSort(uint32_t a[], uint32_t size);
void QuickSort(uint32_t array[], long int left, long int right);
void RadixSort(uint32_t a[], uint32_t n);

#endif
